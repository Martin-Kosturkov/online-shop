spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/store
    username: postgres
    password: postgres

  flyway:
    url: jdbc:postgresql://localhost:5432/store
    user: postgres
    password: postgres
    sql-migration-separator: _